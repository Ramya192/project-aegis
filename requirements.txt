# Runtime dependencies (Streamlit Community Cloud installs this file).
# Deliberately excludes sentence-transformers/torch — reranking uses the Cohere API.

# LLM & Embeddings
openai
langchain-openai
langchain-core
langchain-text-splitters

# Vector Database
qdrant-client

# Reranker (API — zero local RAM)
cohere>=5.0.0

# UI
streamlit

# Utilities
python-dotenv
numpy
pydantic
tiktoken>=0.6.0
